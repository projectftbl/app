var should = require('chai').should()
  , sut = require('../lib/worker');

describe('worker', function() {
  
  var suite = this;

  before(function() {
  });
  
  it('should', function() {
  });
  
});